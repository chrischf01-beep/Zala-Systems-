# Supabase project credentials.
# The anon key is public-safe (RLS protects the data); defaults are already baked
# into src/lib/supabase.ts so the app builds without these. Set them here (or in
# Netlify env vars) only to point at a different project.
# NEVER put the service_role key here or anywhere in the frontend.
VITE_SUPABASE_URL=https://lwkaerkbxovgplucirlv.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
